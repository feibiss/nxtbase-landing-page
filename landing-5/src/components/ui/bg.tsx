import React from "react";

const BackgroundBanner = () => {
  return (
    <div className="fixed inset-0 -z-10 h-full w-full min-h-screen min-w-screen bg-primary-100/20 dark:bg-primary-900/30  dark:from-primary-700 dark:to-secondary-700"></div>
  );
};

export default BackgroundBanner;
